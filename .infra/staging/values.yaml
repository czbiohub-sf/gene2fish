###### Generated using `argus bootstrap` command ######
stack:
  services:
    gene2fish:
      image:
        tag: sha-af9b9fe
      ingress:
        enabled: true
        rules:
          - host: gene2fish.apps-staging.czbiohub.org
      gateway:
        dnsOwner: ingress
        rules:
          - host: gene2fish.apps-staging.czbiohub.org
            vanity:
              enabled: true
